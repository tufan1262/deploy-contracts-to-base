# deploy-contracts-to-base
Deploys core smart contracts to the Base network and stores the deployed addresses in the configuration layer. Updates deployment scripts to support both mainnet and Base environments.
